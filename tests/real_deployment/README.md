# 真实部署测试套件

这是 Auto-Deployer 的真实部署测试套件，使用真实 LLM 测试 Agent 的部署能力。

## 概述

本测试套件通过以下方式评估 Agent：

1. **真实 LLM 测试**：使用真实的 Gemini/OpenAI API，测试 Agent 的智能决策能力
2. **灵活的测试环境**：支持本地测试模式和 Docker 容器测试模式
3. **代表性项目**：使用多个不同类型的项目（Node.js、Python、Docker 等）进行测试
4. **量化指标**：收集成功率、耗时、迭代次数等可量化的指标

## 测试模式

### 🏠 本地测试模式（推荐，默认）

**工作原理**：

- 直接在本地机器上运行部署测试
- 使用 `LocalSession` 执行命令
- 无需创建 Docker 容器

**优点**：

- ✅ **无 Docker in Docker 问题**：可以直接测试需要 Docker 的项目
- ✅ **真实环境**：在真实的开发环境中测试
- ✅ **架构简单**：无需管理容器生命周期
- ✅ **支持所有项目类型**：包括 Docker、Node.js、Python 等

**适用场景**：

- 测试需要 Docker 的项目（如 `docker-welcome`）
- 日常开发和调试
- CI/CD 环境中的集成测试
- 快速验证 Agent 功能

**限制**：

- 在本地环境运行，可能受到已安装软件的影响
- 多次测试可能需要手动清理部署的项目

### 🐳 Docker 容器测试模式

**工作原理**：

- 创建 Ubuntu Docker 容器作为测试目标
- 在容器内安装 SSH 服务器
- Agent 通过 SSH 连接到容器执行部署

**优点**：

- ✅ **完全隔离**：测试环境与宿主机隔离
- ✅ **可重复**：每次测试使用干净的环境

**限制**：

- ❌ **Docker in Docker 问题**：容器内部署 Docker 项目会遇到问题
- ❌ **需要 Docker**：必须安装并运行 Docker
- ❌ **较慢**：创建容器需要额外时间

**适用场景**：

- 需要完全隔离的测试环境
- 测试不需要 Docker 的传统项目
- 验证在干净环境中的部署行为

## 前置要求

### 1. Python 依赖

确保已安装项目依赖：

```bash
pip install -e .
```

### 2. API 密钥

设置 LLM API 密钥：

**Windows (PowerShell)**:

```powershell
$env:AUTO_DEPLOYER_GEMINI_API_KEY = "your-key"
# 或
$env:AUTO_DEPLOYER_OPENAI_API_KEY = "your-key"
```

**Linux/Mac (Bash)**:

```bash
export AUTO_DEPLOYER_GEMINI_API_KEY=your-key
# 或
export AUTO_DEPLOYER_OPENAI_API_KEY=your-key
```

### 3. 本地测试模式额外要求

根据测试项目类型，可能需要：

- **Git**：克隆仓库
- **Node.js & npm**：测试 Node.js 项目
- **Python**：测试 Python 项目
- **Docker**：测试 Docker 项目（如 `docker-welcome`）

### 4. Docker 容器测试模式额外要求

如果使用 Docker 容器测试模式（`--docker`），需要：

**Docker**：确保 Docker 已安装并运行

```bash
# 检查 Docker
docker --version
docker ps
```

**Docker Python 库**：

```bash
pip install docker requests
```

### 4. 测试项目仓库

**重要**：`test_projects.py`中的仓库 URL 需要替换为真实的测试仓库。

当前配置的仓库 URL 是示例，需要：

1. 创建测试项目仓库（或使用现有项目）
2. 更新`test_projects.py`中的`repo_url`字段

## 使用方法

### 本地测试模式（推荐）

运行所有测试项目（本地模式）：

**Windows（推荐使用 Python Launcher）**：

```powershell
# 本地测试模式（默认）
py -3.12 -m tests.run_tests

# 或显式指定本地模式
py -3.12 -m tests.run_tests --local
```

或使用便捷脚本：

```powershell
.\run_tests.ps1
```

**Linux/Mac**：

```bash
# 本地测试模式（默认）
python -m tests.run_tests

# 或显式指定本地模式
python -m tests.run_tests --local
```

### Docker 容器测试模式

如果需要完全隔离的测试环境：

**Windows**：

```powershell
py -3.12 -m tests.run_tests --docker
```

**Linux/Mac**：

```bash
python -m tests.run_tests --docker
```

> **注意**：如果遇到 `ModuleNotFoundError`，请参考 [故障排查指南](../docs/troubleshooting.md) 中的"Python 环境不一致问题"部分。

### 命令行选项

**基本选项**：

```powershell
# 只测试指定项目（本地模式）
py -3.12 -m tests.run_tests --project docker-welcome --local

# 只测试指定难度（本地模式）
py -3.12 -m tests.run_tests --difficulty easy --local

# Docker 容器模式测试指定项目
py -3.12 -m tests.run_tests --project myblog-vitepress --docker

# 使用自定义配置文件
py -3.12 -m tests.run_tests --config config/my_config.json --local
```

**并行测试选项（新功能）**：

```powershell
# 并行测试所有项目（默认3个worker）
python -m tests.real_deployment.test_suite --parallel --local

# 指定并行度（同时运行5个项目）
python -m tests.real_deployment.test_suite --parallel --workers 5 --local

# 并行测试指定难度
python -m tests.real_deployment.test_suite --parallel --difficulty easy --local

# 禁用失败重试
python -m tests.real_deployment.test_suite --parallel --no-retry --local

# 只并行测试指定项目
python -m tests.real_deployment.test_suite --parallel --project docker-welcome --local
```

**并行测试说明**：

- `--parallel`: 启用并行测试模式
- `--workers N`: 设置并行 worker 数量（默认 3，建议 3-5）
- `--no-retry`: 禁用失败自动重试（默认会重试 1 次）
- 并行测试会生成增强的报告，包含系统信息、LLM 配置、重试统计等详细信息
- 测试结果保存在 `tests/results/reports/parallel_test_report_*.json` 和 `*.md`

# 跳过环境设置（调试模式，需要手动提供环境）

py -3.12 -m tests.run_tests --skip-setup

````

**Linux/Mac**：

```bash
# 只测试指定项目（本地模式）
python -m tests.run_tests --project docker-welcome --local

# 只测试指定难度（本地模式）
python -m tests.run_tests --difficulty easy --local

# Docker 容器模式测试指定项目
python -m tests.run_tests --project myblog-vitepress --docker

# 使用自定义配置文件
python -m tests.run_tests --config config/my_config.json --local

# 跳过环境设置（调试模式，需要手动提供环境）
python -m tests.run_tests --skip-setup
````

### 测试项目配置

测试项目在`test_projects.py`中定义。每个项目包含：

- `name`: 项目名称（唯一标识）
- `repo_url`: Git 仓库 URL
- `description`: 项目描述
- `expected_strategy`: 期望的部署策略
- `difficulty`: 难度（easy/medium/hard）
- `verification`: 验证配置（URL、状态码等）

### 添加新测试项目

在`test_projects.py`的`TEST_PROJECTS`列表中添加：

```python
TestProject(
    name="my-new-project",
    repo_url="https://github.com/user/repo.git",
    description="我的新测试项目",
    expected_strategy="docker",
    difficulty="medium",
    expected_time_minutes=8,
    verification=VerificationConfig(
        urls=["http://localhost:3000"],
        expected_status=200
    ),
    tags=["nodejs", "custom"]
)
```

## 测试流程

### 本地测试模式流程

1. **环境准备**：收集本地系统信息，创建工作空间
2. **项目部署**：Agent 在本地执行部署命令
3. **结果验证**：检查部署的应用是否可访问（HTTP 请求）
4. **指标收集**：从日志中提取各种指标
5. **报告生成**：生成 JSON 和 Markdown 格式的报告

### Docker 容器测试模式流程

1. **环境准备**：创建 Docker 容器，安装 SSH 服务器
2. **项目部署**：Agent 连接容器并执行部署
3. **结果验证**：检查部署的应用是否可访问
4. **指标收集**：从日志中提取各种指标
5. **报告生成**：生成 JSON 和 Markdown 格式的报告

## 测试报告

测试完成后，报告保存在`tests/results/reports/`目录：

- `test_report_YYYYMMDD_HHMMSS.json` - JSON 格式（机器可读）
- `test_report_YYYYMMDD_HHMMSS.md` - Markdown 格式（人类可读）

### 报告内容

报告包含：

- **摘要**：总成功率、按难度分类、平均指标
- **详细结果**：每个项目的详细指标
- **策略准确率**：Agent 选择正确策略的比例
- **验证通过率**：部署验证通过的比例

### 关键指标

- **成功率**：部署成功的项目比例
- **部署时间**：完成部署所需的时间
- **迭代次数**：Agent 的决策次数
- **命令数**：执行的命令总数
- **策略准确率**：选择正确部署策略的比例

## 故障排查

### Python 环境不一致

如果遇到 `ModuleNotFoundError: No module named 'dotenv'` 或其他模块找不到的错误，可能是 Python 环境不一致导致的。

**Windows 快速解决**：

```powershell
# 使用 Python Launcher（推荐）
py -3.12 -m tests.run_tests --difficulty easy
```

**详细解决方案**：请参考 [故障排查指南](../../docs/troubleshooting.md) 中的"Python 环境不一致问题"部分。

### Docker 连接失败

**错误信息**：`Error while fetching server API version: (2, 'CreateFile', '系统找不到指定的文件。')`

**原因**：Docker 未安装或未启动。

**解决方案**：

1. **检查 Docker 是否安装**：

```powershell
docker version
```

如果提示命令找不到，需要安装 Docker Desktop：

- Windows/Mac：下载并安装 [Docker Desktop](https://www.docker.com/products/docker-desktop)
- Linux：参考 [Docker 官方文档](https://docs.docker.com/engine/install/)

2. **启动 Docker Desktop**（Windows/Mac）：

   - 确保 Docker Desktop 已启动
   - 等待任务栏图标变为绿色（表示 Docker 引擎已就绪）

3. **验证 Docker 运行状态**：

```bash
# 检查Docker是否运行
docker ps

# 检查Docker版本和连接
docker info
```

4. **临时跳过（仅用于测试）**：
   如果暂时无法安装 Docker，可以使用 `--skip-setup` 跳过环境设置：

```powershell
py -3.12 -m tests.run_tests --skip-setup
```

### SSH 连接失败

- 检查容器是否正常运行：`docker ps`
- 检查 SSH 端口是否被占用
- 查看容器日志：`docker logs autodep-test-env`

### 部署失败

- 查看 Agent 日志：`agent_logs/deploy_*.json`
- 检查测试项目仓库是否可访问
- 验证项目配置是否正确

### 软件源网络问题

**错误信息**：`502 Bad Gateway` 或 `Failed to fetch`

**原因**：Ubuntu 官方软件源网络不稳定或无法访问。

**解决方案**：代码已自动配置使用国内镜像源（阿里云）。如果仍有问题，可以：

1. 检查网络连接
2. 配置代理（如果在公司网络内）

### Docker in Docker 问题

**问题描述**：
在 Docker 容器测试模式中，如果测试项目需要 Docker（如 `docker-welcome`），会遇到"Docker in Docker"（DinD）问题：

- 容器内执行 `docker` 命令失败
- 错误信息：`Cannot connect to Docker daemon` 或 `docker: command not found`

**原因**：
测试容器内部默认没有 Docker，且安装配置 Docker in Docker 复杂且有安全风险。

**解决方案**：
✅ **使用本地测试模式**（推荐）：

```powershell
# 本地模式可以直接使用宿主机的 Docker
py -3.12 -m tests.run_tests --project docker-welcome --local
```

本地测试模式避免了 DinD 问题，可以直接测试需要 Docker 的项目。 3. 手动更换其他镜像源（如清华、网易）

### API 调用失败

- 检查 API 密钥是否正确设置
- 检查网络连接
- 查看 API 配额是否用完

## 配置

配置文件：`tests/real_deployment/config.json`

可以修改：

- `test_mode`: 默认测试模式（`local` 或 `docker`）
- `local_test_environment`: 本地测试环境配置
  - `workspace_dir`: 工作空间目录
  - `cleanup_on_success`: 成功后是否清理
  - `cleanup_on_failure`: 失败后是否清理
  - `preserve_logs`: 是否保留日志
- `test_environment`: Docker 环境配置
  - `base_image`: 基础镜像（默认 `ubuntu:22.04`）
  - `container_name`: 容器名称
  - `ssh_password`: SSH 密码
  - `ssh_timeout`: SSH 超时时间
  - `apt_mirror`: APT 镜像源（默认使用阿里云镜像）
- `deployment`: 部署超时等配置
- `verification`: 验证配置
- `reporting`: 报告生成配置

## 注意事项

1. **测试成本**：每次测试会消耗 LLM API 调用，注意成本
2. **测试时间**：完整测试套件可能需要较长时间（取决于项目数量和复杂度）
3. **环境隔离**：
   - 本地模式：在宿主机运行，可能影响本地环境
   - Docker 模式：使用独立容器，测试完成后自动清理
4. **网络要求**：需要能够访问 GitHub（克隆仓库）和 LLM API
5. **Docker 项目测试**：推荐使用本地测试模式，避免 Docker in Docker 问题

## 示例输出

```
🚀 开始真实部署测试套件
   使用模型: gemini-2.5-flash
   测试项目数: 6

[1/6] 测试项目: nodejs-express-simple
🧪 测试项目: nodejs-express-simple
   仓库: https://github.com/test/express-simple.git
   难度: easy
🚀 开始部署...
✅ 测试完成: 成功
✅ nodejs-express-simple: 成功=True, 耗时=45.2s, 迭代=8

...

📊 测试报告已保存:
   JSON: tests/results/reports/test_report_20251205_131425.json
   Markdown: tests/results/reports/test_report_20251205_131425.md

============================================================
📊 测试报告
============================================================
总项目数: 6
成功: 5 ✅
失败: 1 ❌
成功率: 83.3%

按难度分类:
  easy: 3/3 (100.0%)
  medium: 2/2 (100.0%)
  hard: 0/1 (0.0%)

平均指标（仅成功项目）:
  部署时间: 52.3秒
  迭代次数: 9.2
  命令数: 12.4
  LLM调用: 9.2

策略选择准确率: 100.0%
部署验证通过率: 83.3%
============================================================
```

## 贡献

添加新的测试项目或改进测试套件，请：

1. 在`test_projects.py`中添加项目配置
2. 确保项目仓库可公开访问
3. 更新本文档
